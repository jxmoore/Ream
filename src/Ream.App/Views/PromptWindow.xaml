<Window x:Class="Ream.App.Views.PromptWindow" x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="460" MaxWidth="560" SizeToContent="Height" ResizeMode="NoResize"
        ShowInTaskbar="False" WindowStartupLocation="CenterOwner"
        Style="{StaticResource ModalWindowStyle}">
    <Grid Margin="24,22,24,18">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="GlyphText" Grid.Row="0" Grid.Column="0" Style="{StaticResource RibbonIcon}"
                   FontSize="34" VerticalAlignment="Top" Margin="0,0,18,0"/>

        <ScrollViewer x:Name="MessageScroll" Grid.Row="0" Grid.Column="1" MaxHeight="300"
                      VerticalScrollBarVisibility="Auto" Focusable="False">
            <TextBlock x:Name="MessageText" TextWrapping="Wrap" VerticalAlignment="Center" Margin="0,2,4,0"/>
        </ScrollViewer>

        <StackPanel x:Name="ButtonRow" Grid.Row="1" Grid.ColumnSpan="2" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,22,0,0"/>
    </Grid>
</Window>
